import { OnModuleInit, OnModuleDestroy } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { PrismaService } from '../common/prisma/prisma.service';
import { AiService } from '../ai/ai.service';
import { CategoriesService } from '../categories/categories.service';
import { TransactionsService } from '../transactions/transactions.service';
import { DashboardService } from '../dashboard/dashboard.service';
import { InvoicesService } from '../invoices.service';
import { ClientsService } from '../clients/clients.service';
import { WorkspacesService } from '../common/guards/workspaces.service';
import { RedisCacheService } from '../common/services/redis-cache.service';
import { Context, SessionFlavor } from 'grammy';
interface SessionData {
    state: 'idle' | 'awaiting_category' | 'awaiting_new_category_confirm' | 'awaiting_mode_selection' | 'awaiting_invoice_confirm' | 'awaiting_split_input';
    pendingTransaction: any;
    pendingInvoice: any;
    pendingInvoiceFileUrl: string | null;
    pendingInvoiceBufferBase64: string | null;
    userId: string | null;
    workspaceId: string | null;
}
type MyContext = Context & SessionFlavor<SessionData>;
export declare class TelegramService implements OnModuleInit, OnModuleDestroy {
    private config;
    private prisma;
    private aiService;
    private categoriesService;
    private transactionsService;
    private dashboardService;
    private invoicesService;
    private clientsService;
    private workspacesService;
    private cache;
    private readonly logger;
    private bot;
    private isRunning;
    constructor(config: ConfigService, prisma: PrismaService, aiService: AiService, categoriesService: CategoriesService, transactionsService: TransactionsService, dashboardService: DashboardService, invoicesService: InvoicesService, clientsService: ClientsService, workspacesService: WorkspacesService, cache: RedisCacheService);
    private escapeMarkdown;
    private fetchWithTimeout;
    private getCachedUser;
    private getCachedWorkspaces;
    private getCachedCategories;
    invalidateUserCache(telegramId: string, userId: string): Promise<void>;
    invalidateWorkspaceCache(workspaceId: string): Promise<void>;
    onModuleInit(): Promise<void>;
    onModuleDestroy(): Promise<void>;
    private setupHandlers;
    private handleStart;
    private showModeSelection;
    private handleModeSelection;
    private getTelegramUserAndWorkspace;
    private handleBalance;
    private handleGastos;
    private handleIngresos;
    private handleMainMenu;
    private getWorkspaceBalance;
    private handleAyuda;
    handleInvoiceHelp(ctx: MyContext): Promise<void>;
    handleSplitInput(ctx: MyContext): Promise<void>;
    handleVoiceMessage(ctx: MyContext): Promise<void>;
    handlePhotoMessage(ctx: MyContext): Promise<void>;
    handleDocumentMessage(ctx: MyContext): Promise<void>;
    private transcribeAudio;
    private handleNewCategoryName;
    private handleSplitCalculation;
    private askForCategory;
    private showConfirmation;
    processWebhookUpdate(update: any): Promise<void>;
    sendMessage(telegramId: string, message: string): Promise<void>;
}
export {};
